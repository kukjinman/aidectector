# 구현 프롬프트: 인스타그램 AI 판별 iOS 앱

아래 내용을 그대로 구현 지시(스펙)로 사용한다. 결정된 핵심 방향은 다음 두 가지다.

- 인스타그램 콘텐츠 확보 방식: **공유 확장(Share Extension)**
- AI 생성 여부 판별 방식: **외부 AI 판별 API 연동**

---

## 1. 프로젝트 개요

사용자가 인스타그램 앱에서 게시물(사진/영상/릴스)을 iOS 공유 시트를 통해 이 앱으로 공유하면,
앱이 해당 게시물의 미디어를 추출해 외부 AI 판별 API에 전달하고,
"AI로 생성되었을 확률 몇 %"를 사용자에게 보여준다.

URL을 직접 붙여넣는 방식은 보조 입력 수단으로만 제공한다 (메인 플로우는 공유 확장).

---

## 2. 핵심 사용자 플로우

1. 사용자가 인스타그램 앱에서 게시물을 열고 `공유` 버튼을 누른다.
2. iOS 공유 시트에서 우리 앱(예: "AI 판별기")을 선택한다.
3. 공유 확장이 인스타그램 게시물의 **URL**을 전달받는다.
   - 주의: 인스타그램은 대부분의 경우 이미지/영상 raw 데이터가 아니라 **게시물 링크(URL)** 만 공유 시트로 넘긴다. 실제 이미지 파일 자체를 못 받는 경우를 기본 가정으로 설계한다.
4. 공유 확장은 받은 URL을 앱 본체(또는 백엔드)로 전달하고, 확장 UI에는 "분석 시작됨" 정도의 간단한 확인만 보여준 뒤 빠르게 닫힌다.
5. 백엔드가 해당 URL의 공개 페이지에서 **Open Graph 메타태그** (`og:image`, `og:video`, `og:type`)를 읽어 실제 미디어 URL을 얻는다.
   - 이 방식은 인스타그램이 페이스북/트위터/아이메시지 링크 미리보기용으로 공식적으로 유지하는 데이터라서, HTML 구조를 직접 파싱하는 것보다 훨씬 안정적이다.
   - 로그인 불필요, 비공개 계정 게시물은 미리보기 메타태그가 없거나 제한적일 수 있음 → 이 경우 "비공개 게시물이거나 접근할 수 없는 게시물입니다" 에러로 처리.
6. 추출한 이미지(영상은 대표 썸네일 프레임)를 외부 AI 판별 API로 전송한다.
7. API 응답(AI 생성 확률 or 신뢰도 점수)을 받아 앱 UI에 퍼센티지로 표시한다.
8. 결과는 로컬 히스토리에 저장한다 (원문 URL, 썸네일, 판별 결과, 판별 시각).

---

## 3. 시스템 아키텍처

```
[Instagram App]
     │ (공유 시트)
     ▼
[iOS Share Extension] ──URL──▶ [백엔드 서버] ──og:image/video──▶ [원본 미디어 URL]
     │                              │
     │ (App Group으로 메인 앱에 URL 전달)   │ 미디어 다운로드 후 AI 판별 API 호출
     ▼                              ▼
[메인 앱 (SwiftUI)] ◀── 결과(JSON: 퍼센티지, 라벨, 메타데이터) ───┘
     │
     ▼
[로컬 히스토리 저장 (SwiftData/CoreData)]
```

### 왜 백엔드가 필요한가
- 인스타그램 페이지 요청 시 CORS/차단 정책, User-Agent 검사 등이 있어 클라이언트(iOS)에서 직접 fetch하면 실패율이 높음.
- 외부 AI 판별 API 키를 클라이언트에 노출하면 안 됨 (API 키 유출/과다 호출 위험) → 반드시 서버를 경유.
- 추후 캐싱(같은 URL 재판별 방지), 레이트리밋, 로깅을 서버에서 관리 가능.

---

## 4. 앱 구성 (Xcode 타깃)

1. **메인 앱 타깃** (SwiftUI, iOS 16+)
2. **Share Extension 타깃**
3. 두 타깃 간 데이터 공유를 위한 **App Group** 설정 필수

### 화면 구성 (메인 앱)
- **홈 화면**
  - 안내 문구: "인스타그램 앱에서 게시물을 공유하면 AI 여부를 판별해드려요"
  - URL 직접 붙여넣기 입력창 (보조 수단, 클립보드 자동 감지 붙여넣기 버튼 포함)
  - 최근 판별 히스토리 리스트 (썸네일 + 퍼센티지 + 날짜)
- **분석 중 화면**
  - 로딩 인디케이터 + 단계 표시 (미디어 추출 중 → AI 분석 중)
  - 타임아웃(예: 15초) 시 에러 처리
- **결과 화면**
  - 원본 썸네일
  - 큰 숫자로 "AI 생성 확률 XX%"
  - 확률 구간별 라벨 (예: 0-20% "실제 사진일 가능성 높음", 40-60% "판단 어려움", 80-100% "AI 생성 가능성 높음")
  - 원문 링크로 돌아가기 버튼
  - 결과 공유하기 버튼
- **히스토리 화면**
  - 리스트/그리드, 스와이프 삭제
- **설정 화면**
  - 프라이버시 정책 링크, 앱 버전, (필요 시) API 사용량/구독 정보

### Share Extension UI
- 최소한의 UI만: 공유받은 URL 확인 → "판별하기" 버튼 → 진행 상태 → 완료 시 자동으로 몇 초 뒤 닫히거나 "앱에서 결과 보기" 버튼

---

## 5. 백엔드 API 스펙 (예시)

기술 스택 예시: Node.js(Express/Fastify) 또는 Python(FastAPI), 서버리스(Vercel/AWS Lambda)로 시작 가능.

### `POST /v1/analyze`
**Request**
```json
{
  "url": "https://www.instagram.com/p/XXXXXXXXX/"
}
```

**처리 순서**
1. URL 유효성 검사 (instagram.com 도메인, `/p/`, `/reel/`, `/tv/` 패턴인지 확인)
2. 캐시 확인 (같은 URL을 최근에 분석했는지, Redis/DB로 24시간 캐시)
3. URL의 공개 페이지 요청 (적절한 User-Agent, 타임아웃 5초) → HTML에서 `og:image`, `og:video`, `og:type` 파싱
4. 미디어가 영상이면 대표 프레임 썸네일 URL 사용 (인스타그램은 영상도 `og:image`에 썸네일을 함께 제공하는 경우가 많음)
5. 미디어 다운로드 (크기 제한, 예: 10MB 이하)
6. 외부 AI 판별 API 호출
7. 응답 정규화 후 반환

**Response (성공)**
```json
{
  "status": "ok",
  "aiProbability": 0.87,
  "label": "likely_ai",
  "mediaType": "image",
  "thumbnailUrl": "https://.../thumbnail.jpg",
  "provider": "hive",
  "analyzedAt": "2026-09-11T13:00:00Z"
}
```

**Response (에러)**
```json
{
  "status": "error",
  "code": "PRIVATE_OR_UNAVAILABLE",
  "message": "비공개 게시물이거나 접근할 수 없습니다."
}
```

에러 코드 예시: `INVALID_URL`, `PRIVATE_OR_UNAVAILABLE`, `MEDIA_TOO_LARGE`, `UNSUPPORTED_MEDIA_TYPE`(예: 캐러셀 다중 이미지는 첫 번째만 처리하고 명시), `PROVIDER_ERROR`, `RATE_LIMITED`.

---

## 6. 외부 AI 판별 API 연동

후보 (계약/가격 확인 후 택1, 추후 provider 교체 가능하도록 인터페이스 추상화):

| 서비스 | 특징 |
|---|---|
| Hive Moderation (AI-Generated Content Detection) | 이미지/영상 지원, 정확도 높은 편, 유료 |
| Sightengine | 이미지 판별 API 제공, 문서 잘 정리됨 |
| AI or Not | AI 생성 이미지 판별 전문 API |
| Illuminarty | 이미지 AI 판별 특화 |

구현 지침:
- 백엔드에 `AIDetectionProvider` 인터페이스를 두고, 위 서비스 중 하나를 어댑터로 구현 (추후 다른 서비스로 교체/다중 비교 가능하게).
- 응답 포맷이 서비스마다 다르므로, 반드시 `0.0~1.0` 확률로 정규화해서 클라이언트에 전달.
- API 실패 시 재시도 1회, 그래도 실패하면 `PROVIDER_ERROR` 반환.

---

## 7. 예외 처리 체크리스트

- [ ] 인스타그램 도메인이 아닌 URL 입력 시 즉시 클라이언트 검증으로 차단
- [ ] 비공개 계정/삭제된 게시물
- [ ] 캐러셀(여러 장) 게시물 → 최소 MVP는 첫 번째 이미지만 분석하고 UI에 "여러 장 중 첫 번째만 분석됨" 안내
- [ ] 릴스/영상 → 대표 프레임만 분석하고 "영상은 대표 이미지 기준으로 분석됨" 안내 (영상 전체 분석은 향후 확장)
- [ ] 네트워크 타임아웃/오프라인
- [ ] 같은 URL 반복 요청 시 캐시된 결과 재사용 (서버 비용 절감)
- [ ] Rate limit 초과 시 사용자에게 대기 안내

---

## 8. 법적/정책 고려사항 (구현 시 반드시 인지)

- 인스타그램/Meta 이용약관은 자동화된 콘텐츠 수집(스크래핑)을 제한한다. 본 설계는 전체 페이지 스크래핑이 아니라 **공식적으로 공개된 Open Graph 메타데이터**만 읽는 방식으로 리스크를 낮췄으나, 여전히 완전히 자유로운 것은 아니다.
- 저작권이 있는 타인의 이미지를 서버로 다운로드/전송/분석하는 행위이므로, 원본을 영구 저장하지 않고 분석 목적으로만 임시 사용 후 즉시 폐기하는 정책을 명시한다 (프라이버시 정책 페이지 필요).
- App Store 심사 대비: 앱이 인스타그램 콘텐츠를 "다운로드"하는 것으로 보이지 않도록, 사용자에게 미디어 저장 기능은 제공하지 않고 "판별 결과"만 제공한다는 점을 명확히 한다.
- AI 판별 결과는 100% 정확하지 않으므로, UI에 "참고용 추정치입니다" 같은 디스클레이머를 반드시 표시한다.

---

## 9. 기술 스택 제안

- iOS: Swift, SwiftUI, iOS 16+, App Group, Share Extension (`UIActivityViewController` 아님 — `Share Extension` = `NSExtensionItem` 처리)
- 로컬 저장: SwiftData
- 네트워킹: `URLSession` + `async/await`
- 백엔드: Node.js + Fastify 또는 Python + FastAPI, Redis(캐시), 배포는 Vercel/Fly.io/AWS 중 택1
- AI 판별: 6번 항목의 외부 API 중 택1

---

## 10. 개발 단계 (MVP → 확장)

**Phase 1 (MVP)**
- Share Extension으로 URL 수신 → 백엔드 → og:image 추출 → 외부 API 판별 → 퍼센티지 표시
- URL 직접 붙여넣기 보조 입력
- 로컬 히스토리 (저장만, 상세 기능 없음)

**Phase 2**
- 캐러셀 다중 이미지 각각 분석 후 평균/최댓값 표시
- 결과 공유 카드 이미지 생성 기능
- 캐시/레이트리밋 강화

**Phase 3**
- 영상 프레임 여러 개 샘플링해 평균 분석 (단순 썸네일 1장 이상)
- 다중 AI 판별 프로바이더 결과 비교/앙상블

---

## 11. 다음 단계

이 문서를 확정 스펙으로 삼아 Phase 1(MVP)부터 실제 코드(Xcode 프로젝트 + 백엔드)를 구현한다.
