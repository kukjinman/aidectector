# Backend
backend/node_modules/
backend/dist/
backend/.env

# Xcode — the project is generated by `xcodegen generate` from ios/project.yml,
# so the .xcodeproj itself isn't committed (avoids pbxproj merge conflicts).
ios/AIDetector.xcodeproj/
ios/build/
ios/DerivedData/
*.xcuserstate

# macOS
.DS_Store

backend/data/
backend/certs/

ios/build-device/
